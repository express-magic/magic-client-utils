import {each, hasLocalStorage} from './utils';
import css from './css';
import dom from './dom';
import http from './http';

var utils = {
    hasLocalStorage
  , each
  , forEach: index.each
  , css
  , dom
  , http
};

export default utils;
